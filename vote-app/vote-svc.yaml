apiVersion: v1
kind:  Service
metadata:
   name: vote-app-svc
   labels: 
     name: vote-app-svc
     app: votingapp
spec: 
  type: LoadBalancer
  ports:
    - targetPort: 80
      port: 80
  selector: 
      name: vote-app-pod
      app: votingapp
  